import React from "react";
import Plot from "react-plotly.js";

const Topics = () => {
    return (
        <div>
            <Plot data={[]} layout={{}} config={{}} />
        </div>
    );
};

export default Topics;
